# api/config/schema.yaml
# The list of types and properties we want to use
types:
    # Parent class of Person
    Recipe:
        properties:
            name: ~
            description: ~
            tags: ~
    Ingredient:
        properties:
            name: ~